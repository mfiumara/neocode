# Workspace controlled by the coordinator and workers
NEOCODE_CWD=/path/to/project

# Local orchestration server port
NEOCODE_PORT=4318

# Completion pipeline (successful worktree workers only)
NEOCODE_MERGE_BRANCH=main
# Optional; defaults to the coordinator model
NEOCODE_JUDGE_MODEL=provider/model-id
# Optional; otherwise package.json test/check/build scripts are detected
NEOCODE_CI_COMMAND="npm test && npm run check && npm run build"
NEOCODE_CI_TIMEOUT_MS=300000

# Interrupted worker automatic recovery
NEOCODE_WORKER_RESTART_MAX_RETRIES=3
NEOCODE_WORKER_RESTART_BACKOFF_MS=1000
NEOCODE_WORKER_RESTART_MAX_BACKOFF_MS=30000

# Conservative merged-worktree cleanup (defaults: 7 day grace, every 6 hours)
NEOCODE_JANITOR_GRACE_MS=604800000
NEOCODE_JANITOR_INTERVAL_MS=21600000
NEOCODE_JANITOR_TARGET_REF=main
NEOCODE_JANITOR_STARTUP=true
# Idle coordinator backlog sweep fallback (event-driven sweeps run sooner)
NEOCODE_SWEEP_INTERVAL_MS=30000
